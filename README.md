# cpp
Simple hello world program

# dependencies
cmake, g++ (Linux/Unix), MSVC (Windows)

# building
### build and run
```console
mkdir build && cd build
cmake ..
make
```

### manual run
inside build dir
```console
./src/hello_world
```
